#include <iostream> 
#include <vector> 
using namespace std; 
int main() {

    int nc; cin >> nc; 
    int n, k, ref;

    for (int i = 0 ; i < nc ; i++) 
    {
        cin >> n; cin >> k;
        vector<int> pp; 

        for (int j = 1 ; j <= n ; j++)
        {
            pp.push_back(j); 
        }
        for (int j = 0 ; j < n ; j++)
        {
            
        }


    }

    return 0; 
}